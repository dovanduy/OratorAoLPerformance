using System;

namespace Profiler.LogManager.Models
{
    public enum ValueType
    {
        NOT_APPLICABLE,
        OBJECTS,
        BYTES,
        MILLISECONDS,
        ALLOCATIONS,
        ID,
        PERCENT
    }
}

